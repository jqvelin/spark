// hooks are exported from a dedicated index.ts file inside /hooks folder
// to avoid exporting server and client logic from the same file
export { getSongsCollectionDuration } from "./getSongsCollectionDuration";
